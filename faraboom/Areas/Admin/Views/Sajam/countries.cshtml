@model ViewModels.AdminViewModel.sajam.Vm_countries;
@{
    Layout = "_Layout4" ;
}

<form action="/Admin/Sajam/Showcountries" method="POST">
	<div class="card shadow">
		<div class="card-header">
			<h2 class="mb-0"> لیست کشور ها </h2>
		</div>
		<div class="card-body">
			<div class="row">
				@* <div class="form-group">
				<h2 class="mb-0"> شناسه : </h2>
				<input type="text" class="form-control" name="input" placeholder="شناسه ">
				</div> *@
			</div>
			<button type="submit" class="btn btn-success  mt-1 mb-1" style="width: min-content;"> بررسی کشورها
			</button>
			<a href="~/Admin/Sajam/Index" class="btn btn-success mt-1 mb-1" style="width: min-content;">
				بازگشت
			</a>
		</div>
	</div>
	<p id="tok" style="display:none;">@ViewBag.result</p>
	<input style="display:none;" asp-for="token">
	<p id="idcount" style="display:none;">@ViewBag.result</p>
	<input style="display:none;" asp-for="country_id">
</form>

<script>

	debugger
	var str = document.getElementById('tok').innerText.replace("{", "").replace(' \" ', '').replace("}", "").split('"').join('');
	var res = str.split(",");

	var bill_id = res[1].split(":");
	document.getElementById('token').value = bill_id[1];

	var str = document.getElementById('idcount').innerText.replace("{", "").replace(' \" ', '').replace("}", "").split('"').join('');
	var res = str.split(",");

	var bill_id = res[2].split(":");
	document.getElementById('country_id').value = bill_id[2];



</script>